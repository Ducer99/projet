[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirects pour React Router (SPA) - toutes les routes → index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
